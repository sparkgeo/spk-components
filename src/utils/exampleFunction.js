/**
 * Example function that always returns true.
 *
 * This function demonstrates a simple arrow function
 * that returns a boolean value.
 *
 * @description
 * A brief description of what the function does.
 *
 * @returns {boolean}
 *     Always returns true.
 *
 * @example
 * import { exampleFunction } from "../../utils";
 * 
 * const result = exampleFunction();
 * console.log(result); // Output: true
 */
export const exampleFunction = () => true